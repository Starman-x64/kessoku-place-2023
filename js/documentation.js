// load and parse all chapter json files into objects
  // make an object whose keys are the name of each chapter, and values are the chapter objects
// load and parse all event json files into objects

// loop through each event and
  // add a reference to it to it's first listed chapter entry
  // replace string references of parent chapters with references to 

// loop through each chapter and add it and its events to the screen